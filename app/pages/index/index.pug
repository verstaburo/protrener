extends ../../layouts/onescreen

block head
  - var pageTitle = 'Protrener - Главная'

block script
  script
    | document.documentElement.className = document.documentElement.className.replace('no-js', 'js');

block content
  +banner._fixed
  +b.index
    +page-section
      +col(24).container__inner
        +sport-navigation
