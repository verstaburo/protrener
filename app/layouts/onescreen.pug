include _externalIncludes
include _internalIncludes

doctype html
html.no-js(lang='ru')
  include head
  body
    .wrapper.wrapper_onescreen
      +video-bg
      .valve
      .page-content.page-content_onescreen
        +header(false, true)._abs
        .content#main-content
          block content
        +brushes-animate._layers
        +footer-contacts(true)._fixed
    +popups
      +search-popup#search
    script(src='assets/scripts/libs/ResizeSensor.js')
    script(src='assets/scripts/app.min.js')
    script(src='assets/scripts/main.js')
